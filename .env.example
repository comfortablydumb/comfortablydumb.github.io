# Copy to .env and fill in. All PUBLIC_ vars are exposed to the client at build time.

# --- Analytics (optional; pick one or none) ---
# Plausible: your verified domain, e.g. samarthjindal.com
PUBLIC_PLAUSIBLE_DOMAIN=
# Cloudflare Web Analytics beacon token (from the CF dashboard)
PUBLIC_CF_BEACON_TOKEN=
